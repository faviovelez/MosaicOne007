json.extract! discount_rule, :id, :percentage, :product_list, :prospect_list, :product_id, :initial_date, :final_date, :user_id, :rule, :minimum_amount, :minimum_quantity, :exclusions, :active, :business_unit_id, :store_id, :created_at, :updated_at
json.url discount_rule_url(discount_rule, format: :json)
