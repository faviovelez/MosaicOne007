<p id="notice"><%= notice %></p>
<% @assigned_to_others.each do |request| %>

    <table>
      <thead>
        <tr>
          <th>tienda</th>
          <th>cantidad</th>
          <th>producto</th>
          <th>prospecto</th>
          <th>creada hace</th>
          <th>fecha compromiso</th>
          <th>estatus</th>
          <th>atendida por</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td> <%= request.store.store_code %> - <%= request.store.store_name %> </td>
          <td><%= request.quantity %></td>
          <td>
            <% if request.product_type == 'otro'%>
              <%= request.name_type %>
            <% elsif request.product_type != 'otro'%>
              <%= request.product_type %>
            <% end %>
          </td>
          <% if request.prospect.present? %>
            <td><%= request.prospect.legal_or_business_name %></td>
          <% else %>
          <td> - </td>
          <% end %>
          <% @days = (Date.today - request.created_at.to_date).to_i %>
          <% if @days < 1 %>
            <td> hoy </td>
          <% elsif @days == 1 %>
            <td> 1 día </td>
          <% else @days > 1 %>
            <td> <%= @days %> días </td>
          <% end %>
          <% if request.delivery_date.present? %>
            <td> <%= I18n.l request.delivery_date, format: :short %></td>
          <% else %>
          <td> - </td>
          <% end %>
          <td> <%= request.status %></td>
          <td> <% find_manager(request) %>
              <%= @follower.first_name %>
              <%= @follower.last_name %>
            </td>
          <td> <%= link_to 'detalles', request %> </td>
        </tr>
      </tbody>
    </table>
<% end %>

<%= render 'links_for_manager' %>
