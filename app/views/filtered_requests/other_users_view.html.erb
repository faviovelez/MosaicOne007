<p id="notice"><%= notice %></p>
<% @assigned_to_others.each do |request| %>

    <table>
      <thead>
        <tr>
          <th>tienda</th>
          <th>cantidad</th>
          <th>producto</th>
          <th>prospecto</th>
          <th>creada hace</th>
          <th>fecha compromiso</th>
          <th>estatus</th>
          <th>atendida por</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td> <%= request.store.store_code %> - <%= request.store.store_name %> </td>
          <td><%= request.quantity %></td>
          <td>
            <% if request.product_type == 'otro'%>
              <%= request.name_type %>
            <% elsif request.product_type != 'otro'%>
              <%= request.product_type %>
            <% end %>
          </td>
          <% if request.prospect.present? %>
            <td><%= request.prospect.legal_or_business_name %></td>
          <% end %>
          <% @days = (Date.today - request.created_at.to_date).to_i %>
          <% if @days < 1 %>
            <td> hoy </td>
          <% elsif @days == 1 %>
            <td> 1 día </td>
          <% else @days > 1 %>
            <td> <%= @days %> días </td>
          <% end %>
          <% if request.delivery_date.present? %>
            <td> <%= request.delivery_date %></td>
          <% else %>
          <td> - </td>
            <% end %>
          <td> <%= request.status %></td>
          <% id = 0 %>
          <% request.users.each do |user| %>
            <% if user.role.name == 'manager' %>
              <% id = user.id %>
            <% end %>
            <td>
              <%= request.users.find(id).first_name %>
              <%= request.users.find(id).last_name %>
            </td>
          <% end %>
          <td> <%= link_to 'detalles', request %> </td>
        </tr>
      </tbody>
    </table>
<% end %>
