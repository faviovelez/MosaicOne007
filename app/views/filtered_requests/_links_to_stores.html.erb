<%= link_to 'ver prospectos', store_prospects_path(current_user.store) %>

<% unless params[:action] == 'current_view' %>
  <%= link_to 'solicitudes activas', filtered_requests_current_view_path %>
<% end %>

<% unless params[:action] == 'inactive_view' %>
  <%= link_to 'solicitudes inactivas', filtered_requests_inactive_view_path %>
<% end %>

<% unless params[:action] == 'saved_view' %>
  <%= link_to 'solicitudes guardadas', filtered_requests_saved_view_path%>
<% end %>
