<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <div class="left">
        <%= link_to root_path do %>
          <%= image_tag '/assets/logo-disenos-decarton.png', alt: 'Logo Diseños de Cartón', class:'logo img-responsive'  %>
        <% end %>
        <% if current_user %>
          <p class="greeting">Bienvenido(a), </p>
            <p class="username">
            <%= current_user.first_name %>
            <%= current_user.last_name %>
          </p>
        <% end %>
      </div>
    </div>


    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
            <% if current_user.nil? %>
              <li>
                <%= link_to 'Ingresa', new_user_session_path %>
              </li>

              <li>
                <%= link_to 'Regístrate', new_user_registration_path %>
              </li>
            <% else %>
            <li>
              <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete %>
            </li>
            <li>
              <%= link_to 'Cambia tu contraseña', edit_user_registration_path %>
            </li>
            <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
