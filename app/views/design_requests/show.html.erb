<%= render 'info' %>

<%= render 'documents_for_design_request' %>

<% if @design_request.status == 'solicitada' && current_user.role.name == 'designer' %>
  <%= render 'form_to_assign' %>
<% end %>

<%= render 'modal_for_design' %>

<%= render 'links_for_design_request' %>
