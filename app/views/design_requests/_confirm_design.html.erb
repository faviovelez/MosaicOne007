<p class="individual-font"> Acepto el costo de $<%=@design_request.cost %> pesos + IVA para esta solicitud.</p>

<%= form_for(@design_request) do |f| %>

    <div class="field form-inside-style justify">
      <%= f.label :confirmar_diseño %>
      <%= f.check_box :status, { multiple: true }, 'aceptada', nil %>
    </div>

    <div class="field form-inside-style hidden">
      <%= f.label :descripción_de_la_solicitud %>
      <%= f.text_area :description, class: 'form-inside' %>
    </div>

    <div class="actions">
      <%= f.submit :aceptar_diseño, name: 'aceptar_diseño', class: 'main-button capitalize' %>
    </div>

<% end %>
