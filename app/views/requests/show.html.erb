<%= render 'info' %>

<%= render 'documents_for_request' %>

<%= render 'modal' %>

<% if current_user.role.name == 'product-admin'%>
  <%= render 'product_admin_form' %>
<% end %>

<%= render 'links_to_show' %>
