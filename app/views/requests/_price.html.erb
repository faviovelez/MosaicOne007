<%= form_for(@request) do |f| %>

  <div class="field">
    <%= f.label :precio_de_venta %>
    <%= f.text_field :sales_price %>
  </div>

  <div class="actions">
    <%= f.submit :asignar_precio, name: 'asignar_precio' %>
  </div>

<% end %>
