<%= form_for(@request) do |f| %>

  <div class="field hidden">
    <%= f.text_field :product_what %>
  </div>

  <div class="actions">
    <%= f.submit :asignar, name: 'asignar' %>
  </div>
<% end %>
