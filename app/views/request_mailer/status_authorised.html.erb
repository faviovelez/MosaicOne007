<% @value = '' %>
<% if @request.product_type == 'otro' %>
  <% @value = @request.name_type %>
<% else %>
  <% @value = @request.product_type %>
<% end %>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Precio de Venta asignado</title>
  </head>
  <body>
    <p>
      Estimado(a) <%= @manager.first_name %> <%= @manager.last_name %>.
      La tienda <%= @request.store.store_code %> - <%= @request.store.store_name %> ha confirmado la solicitud número <%= @request.id %> por <%= @request.quantity %> piezas.
      Tipo de producto: <%= @value %>.
    </p>
  </body>
</html>
