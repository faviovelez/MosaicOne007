<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Precio de Venta asignado</title>
  </head>
  <body>
    <p>
      Estimado(a) <%= @manager.first_name %> <%= @manager.last_name %>.
      La tienda <%= @request.store.store_code %> - <%= @request.store.store_name %> ha asignado un precio de venta de <%= number_to_currency(@request.sales_price) %> pesos a la solicitud número <%= @request.id %>.
    </p>
  </body>
</html>
