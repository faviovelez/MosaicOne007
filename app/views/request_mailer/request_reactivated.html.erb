<% @value = '' %>
<% if @request.product_type == 'otro' %>
  <% @value = @request.name_type %>
<% else %>
  <% @value = @request.product_type %>
<% end %>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Solicitud reactivada</title>
  </head>
  <body>
    <p>
      Estimado(a) <%= @manager.first_name %> <%= @manager.last_name %>.
      La tienda <%= @request.store.store_code %> - <%= @request.store.store_name %> ha reactivado la solicitud número <%= @request.id %> por <%= number_with_delimiter(@request.quantity) %> piezas, el estatus es <%=@request.status %>.
      Tipo de producto: <%= @value %>.
    </p>
  </body>
</html>
