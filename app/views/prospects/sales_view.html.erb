<table id="store_movements" class="table-display">
  <thead>
    <tr class="table-titles">
      <th> Nombre </th>
      <th> Primer venta </th>
      <th> Última venta </th>
      <th> Descuentos </th>
      <th> Ventas </th>
      <th> Total venta </th>
      <th> Total pago </th>
      <th> Saldo </th>
      <th> Piezas vendidas </th>
      <th> Venta promedio </th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td> <%= @prospect.legal_or_business_name %> </td>
        <% if @prospect.first_purchase == 'Sin compras' %>
          <td> <%= @prospect.first_purchase %> </td>
          <td> <%= @prospect.last_purchase %> </td>
        <% else %>
          <td> <%= l @prospect.first_purchase %> </td>
          <td> <%= l @prospect.last_purchase %> </td>
        <% end %>
        <td> <%= number_to_currency(@prospect.total_discounts) %> </td>
        <td> <%= number_with_delimiter(@prospect.sales_number) %> </td>
        <td> <%= number_to_currency(@prospect.total_sales) %> </td>
        <td> <%= number_to_currency(@prospect.total_payments) %> </td>
        <td> <%= convert_balance(@prospect) %>  </td>
        <td> <%= number_with_delimiter(@prospect.total_units) %> </td>
        <td> <%= number_to_currency(@prospect.average_sale) %> </td>
      </tr>
  </tbody>
</table>

<table class="hidden">
  <tbody>
    <tr>
      <td id="date"> <%= l Date.today, format: :short %> </td>
      <td id="store"> <%= current_user.store.store_name %> </td>
      <td id="hour"> <%= Time.now.strftime("%H:%M") %> </td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

<%= content_for :stylesheet do %>
  <%= stylesheet_link_tag 'datatables.min' %>
<% end %>

<%= content_for :javascript do %>
  <%= javascript_include_tag 'pdfmake.min' %>
  <%= javascript_include_tag 'vfs_fonts.js' %>
  <%= javascript_include_tag 'datatables.min.js' %>
  <%= javascript_include_tag 'movements-day' %>
<% end %>
