<% if params[:request_id] %>
  <% if Request.find(params[:request_id]).product == nil%>
    <%= render 'create_from_request' %>
  <% else %>
    <% controller.redirect_to root_path, notice: 'El producto ya fue creado' %>
  <% end %>
<% else %>
  <%= render 'form' %>
<% end %>
