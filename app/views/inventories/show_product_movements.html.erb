<table id="store_movements" class="table-display">
  <thead>
    <tr>
      <th colspan="6" class="table-titles"> Resumen de movimientos <%= params[:date] %> </th>
    </tr>
    <tr>
      <th> Código </th>
      <th> Descripción </th>
      <th> Fecha </th>
      <th> Hora </th>
      <th> Movimiento </th>
      <th> Cantidad </th>
    </tr>
  </thead>
  <tbody>
    <% @movements.each do |movement| %>
        <tr>
          <td> <%= movement.product.unique_code %> </td>
          <td> <%= movement.product.description %> </td>
          <td> <%= l movement.created_at.to_date %> </td>
          <td> <%= movement.created_at.strftime("%I:%M%p") %> </td>
          <td> <%= movement.movement_type %> </td>
          <td> <%= movement.quantity %> </td>
        </tr>
    <% end %>
  </tbody>
</table>

<table class="hidden">
  <tbody>
    <tr>
      <td id="date"> <%= l Date.today, format: :short %> </td>
      <td id="store"> <%= current_user.store.store_name %> </td>
      <td id="hour"> <%= Time.now.strftime("%H:%M") %> </td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

<%= content_for :stylesheet do %>
  <%= stylesheet_link_tag 'datatables.min' %>
<% end %>

<%= content_for :javascript do %>
  <%= javascript_include_tag 'pdfmake.min' %>
  <%= javascript_include_tag 'vfs_fonts.js' %>
  <%= javascript_include_tag 'datatables.min.js' %>
  <%= javascript_include_tag 'movements-day' %>
<% end %>
