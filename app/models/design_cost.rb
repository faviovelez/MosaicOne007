class DesignCost < ActiveRecord::Base
  # Este debe guardar todos los costos, solo debe tener complejidad y costo para que el diseñador elija el grado de complejidad y eso determine el costo de la solicitud de diseño.
end
